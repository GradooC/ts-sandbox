{
    "compilerOptions": {
        "lib": ["DOM", "ES2020"],
        // "downlevelIteration": true,
        "module": "commonJS" // It allows to use import/export syntax in the .spec. files
    },
    "include": ["src/**/*"]
}
