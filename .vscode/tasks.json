{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "npx",
            "args": ["ts-node", "${relativeFile}"],
            "label": "Start",
            "detail": "Run current file"
        }
    ]
}
